<%# <% unless feedback.archived? %> 

  <p>
    <strong>Commenter:</strong>
    <%= feedback.commenter %>
  </p>

  <p>
    <strong>Feedback:</strong>
    <%= feedback.body %>
  </p>
    <p>
    <%= link_to "Delete", [feedback.student, feedback], data: {
                  turbo_method: :delete,
                  turbo_confirm: "Are you sure?"
                } %>
  </p>

<%# <% end %> 
