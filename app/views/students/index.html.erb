<ul>
  <% @students.each do |student| %>  
      <li>
        <%= link_to student.name, student %> <br>
      </li>
    <% end %>
</ul>
<%= link_to "New Student", new_student_path %>
  <%# <h1>modify from student branch</h1> %>